import type { Request, Response, NextFunction } from 'express';
import { container } from "tsyringe";
import { {{pascalCase module_name}}{{pascalCase useCase_name}}UseCase } from "./{{camelCase module_name}}.{{camelCase useCase_name}}.useCase"

export class {{pascalCase module_name}}{{pascalCase useCase_name}}Controller {

  async handler(req: Request, res: Response, _next: NextFunction): Promise<void> {
    
    const {{camelCase module_name}}{{pascalCase useCase_name}}UseCase = container.resolve({{pascalCase module_name}}{{pascalCase useCase_name}}UseCase)
    const result = await {{camelCase module_name}}{{pascalCase useCase_name}}UseCase.execute({})

    res.status(200).json(result);
  }
}
